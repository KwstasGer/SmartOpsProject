@model SmartOpsProject.Models.Service
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Διαγραφή Υπηρεσίας";
}

<h2 class="text-danger">Διαγραφή Υπηρεσίας</h2>
<p>Είστε σίγουροι ότι θέλετε να διαγράψετε την παρακάτω υπηρεσία;</p>

<div class="card mb-3">
    <div class="card-body">
        <h5 class="card-title">@Model.ServiceCode — @Model.Description</h5>
        <p class="card-text mb-1"><strong>Μ.Μ.:</strong> @Model.Unit</p>
        <p class="card-text mb-1"><strong>ΦΠΑ:</strong> @Model.VAT</p>
        <p class="card-text mb-1"><strong>Λιανική:</strong> @Model.RetailPrice</p>
        <p class="card-text mb-0"><strong>Χονδρική:</strong> @Model.WholesalePrice</p>
    </div>
</div>

<form asp-action="Delete" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Οριστική Διαγραφή</button>
    <a asp-action="Index" class="btn btn-outline-secondary">Ακύρωση</a>
</form>
